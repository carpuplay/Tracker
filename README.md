# Portable GPS Tracker with Remote Alarm
L'objectif de ce projet est de construire un système anti-vol pour les vélos.
Le systeme devra upload sa position a un serveur, et que celle ci soit consultable sur un site web, si il est deplace alors que le systeme est bloqué, alors il devra envoyer un sms.
